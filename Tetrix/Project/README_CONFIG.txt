This directory contains a .erlang, which is used for automatically launching applications.

Currently, it contains:
application:start(tetrix).

This launches the main supervisor server, 3 child servers, and 4 workers
